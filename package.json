{
  "name": "root",
  "private": true,
  "scripts": {
    "prettier": "prettier --write \"packages/*/src/**/*.{ts,tsx,js,jsx}\"",
    "deploy": "firebase deploy",
    "deploy-hosting": "firebase deploy --only hosting",
    "deploy-functions": "firebase deploy --only functions"
  },
  "devDependencies": {
    "lerna": "^3.18.3",
    "prettier": "^1.18.2"
  }
}
