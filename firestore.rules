rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
  	match /users/{userId}/linkedProviders/{provider} {
    	allow read, delete: if userId == request.auth.uid;
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
